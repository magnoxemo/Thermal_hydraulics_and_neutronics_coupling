def neutron_locator(x,y,z):
  
	def search(x,y):
    #will write it's details when my exams are over 
    pass 
  
  def material_id(location_id):
     #will write it's details when my exams are over 
    pass 
 	
	x=x
	y=y
	z=z
  
	if np.sqrt(x**2+y**2)>core_dim:
		neutron_life=False
	elif z>core_dim:
		neutron_life=False
		break
	else 
		location_id=search(x,y)
		material_id=material_search(location_id)
    return material_id
